"""Stage 5: Merge — cross-batch dedup, ALD dedup, naturesense classification."""

from __future__ import annotations


async def run_merge(issuer_id: str, config) -> int:
    """Deduplicate extracted assets and write to discovered_assets. Returns count."""
    raise NotImplementedError("Phase 2 — Task 2.4")
